# Production URL of the token-screener app. Sourced from an env var (not
# hardcoded) so the "Enter App" button can be repointed later if domains are
# consolidated onto this coming-soon page's domain.
VITE_APP_URL=https://robinhood-chain-screener-token-scre.vercel.app
